---
title: Sketchy Sermons
---
{% include '_header' %}
    <header id="site-header">
      <a class="logo" href="/">
        <img src="https://sketchysermons.com/wp-content/themes/twentytwenty-child/assets/images/sketchy-header-v2.png?v=87982739478" />
      </a>
      <nav>
      {% include '_navigation' %}
      </nav>
    </header>
    <div id="content">
      <header>
          <h1>{{ title }}</h1>
          {% if featuredImage != null %}
          <aside id="featured-container">
              <img id="featured-image" src="{{ featuredImage }}" alt="Cover Image for {{ title }}" />
          </aside>
          {% endif %}
      </header>
      <article>
      {{ content }}

      {% if download != null %}
        <div class="download-container">
          <a href="{{ download }}">Download</a>
        </div>
        
      {% endif %}

      <ul class="tags">
        {%- for tag in tags -%}
          <li><a href="/tag/{{ tag }}">{{ tag }}</a></li>
        {%- endfor -%}
      </ul>

      </article>
      <footer>
        {% include 'author' %}
      </footer>
    </div>
    
    {% include '_footer' %}

  </body>
</html>